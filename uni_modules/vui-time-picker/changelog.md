## 1.5.0
- **`min` / `max` 先收成组件自己的精度再比较**：边界允许写成 `HH:mm` 或 `HH:mm:ss`，而组件的精度由
  `showSeconds` 决定，直接比字符串会错 —— `max="14:30:00"` 会让一个**不显示秒**的组件吐出带秒的值
  （父组件收到的字符串与界面上看到的不是一个精度），`min="09:00:30"` 同理。
  现在不显示秒时上界向下取整、下界向上进位，两个方向都保证「收完之后仍然不越界」

## 1.0.0
- 初版发布，实现 VUI 时间选择器组件

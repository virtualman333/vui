## 1.4.1
- **`current` 同时收上下界**：此前只收下界，条数变少之后（筛选、删掉一行）父组件手里的页码可能
  比总页数大，而页码列表里的页号最大只到 `pageCount` —— 两边对不上时**一页都不会被高亮**，
  用户看到的是「一页都没选中」；点「上一页」还会从那个越界值减 1，一次点击直接跳到最后一页。
  收窄只影响展示与高亮，`update()` 抛给父组件的值不变

## 1.0.0
- 初版发布，实现 VUI 分页组件

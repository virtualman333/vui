# vui-message

VUI 消息提示组件

## 属性
- modelValue 是否展示，支持 v-model
- message 消息内容
- type primary / success / warning / error / info
- duration 自动关闭时间，0 为不关闭
- show(msg, type) 通过 ref 调用

## 使用示例

```html
<vui-message />
```

更多示例请参考项目演示页 `pages/demo/demo.vue`。

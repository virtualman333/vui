# vui-drawer

VUI 抽屉组件

## 属性
- modelValue 是否展示，支持 v-model
- position left / right / top / bottom
- width 宽度（左右）
- height 高度（上下）
- mask 是否显示遮罩
- maskClosable 点击遮罩是否关闭

## 使用示例

```html
<vui-drawer />
```

更多示例请参考项目演示页 `pages/demo/demo.vue`。
